# BMS Database 贡献指南

BMS Database 面向浙江大学基础医学本科生，重点记录课程怎样考核、平时需要完成什么、怎样复习，以及哪些资料真正有用。课程介绍可以保留，但不应压过学习经验、考核和资料入口。

发现错误、链接失效或愿意提供学习记录，可以先阅读网站的[贡献与纠错](https://bms-zju.github.io/BMS_Database/contribute/)；不必先安装开发环境或整理成网页。本指南面向直接修改仓库的人。

## 文档分工

[README.md](README.md) 是项目介绍、使用与参与入口，保留必要的资料许可边界和本地运行方法。具体编辑规则放在下列对应指南中；更新时修改现行条目，不把内部维护记录或每轮实施方案追加到 README，也不在多份文档重复维护同一套细则。资料使用要求以[来源与使用范围](docs/contribute/sources-and-permissions.md)及具体材料说明为准，文档重组不改变既有许可。

## 开始之前

一次只处理一门课程或一个明确问题。修改前应：

1. 查看当前 Git 状态，保留并绕开不属于本次范围的修改；
2. 阅读现有课程页面和相关原始材料；
3. 涉及课程主页时阅读 [课程主页维护指南](COURSE_HOMEPAGE_GUIDE.md)；
4. 涉及试卷或小测时阅读 [回忆卷制作规范](RECALL_PAPER_GUIDE.md)，学年、学期和考试类型按 [学期与校历参考](EXAM_SEMESTER_REFERENCE.md) 核对；
5. 调整字体、字号或阅读交互时阅读 [阅读体验维护指南](READING_EXPERIENCE_GUIDE.md)；
6. 先判断文件从属关系，再决定编辑、迁移或删除。

不要使用已经停用的固定课程模板，也不要为了栏目完整补写材料中没有的课程事实或通用建议。

## 证据与写作

- 课程通知、课程导航、教学日历、课件和原始任务要求优先用于确认课程事实。
- 学习建议只从能够追溯的个人经验中整理，不把个人感受写成普遍规则。
- 分数、题型、任务次数和开闭卷方式应注明适用学年、年级或班次。
- 无法确认的内容不得猜测。正式页面没有可靠内容的栏目直接省略，核查事项留在维护记录或 PR 说明中。
- 回忆卷应保留缺题、缺图、存疑术语和回忆者说明，不自行补成标准答案。
- 页面正文可以合并多份经验以方便阅读；作者、学年、原始位置和使用边界应在维护记录或 PR 说明中保留。
- 标有“请勿转载”、仅限课程群或含个人信息的材料只能用于内部核对，不加入公开下载。
- 正文只继承已确认的事实和要求，不继承纠错过程，应让未看过修改对话的读者独立理解。用户纠正用于更新写作依据，不自动变成澄清、强调或免责声明；逐段删除或重写只回应修改过程的解释，不机械删词。
- 已确认的事实优先直接陈述；对比、否定和提醒只在帮助理解或避免实际误用时保留。不得为简洁抹去适用学年、课程区别与替代关系、教学与考核要求、来源、缺失、存疑和使用限制等必要边界。

## 文件处理

- PDF、PPTX 和 DOCX 在提取后仍要检查图片、表格、二维码和版式信息。
- 原文件完成核对前不得删除；移动或删除前应确认已有可靠副本，并在需要时核对 SHA-256。
- 不把未定稿笔记、浏览器 profile、临时预览或内部维护记录放进 `docs/`。
- 讨论课放入 `discussions/`，小测放入 `quizzes/`，考试资料放入 `exams/`，下载文件放入 `downloads/`，课程图片放入 `assets/`。
- 路径使用小写英文和连字符，学年统一写作 `2025-2026`；已有旧目录不做无关的批量重命名。

并非每门课程都需要建立所有子目录。没有相应资料时，不建立空目录。

## 页面与导航

- 每门课程保留一个 `index.md` 主页。
- 课程主页的站内资料分组使用 `course-links`，即使只有一个分组入口；站外资源和站内单篇资料使用普通列表，具体见 [课程主页维护指南](COURSE_HOMEPAGE_GUIDE.md#资料入口与外观)。已核对的分数构成可以使用 `course-score`。
- 不为单门课程创建独立视觉体系，除非现有组件确实无法表达内容。
- 未列入 `nav` 的文件仍可能被 MkDocs 发布。草稿、源码片段和内部文件必须分别使用 `draft_docs`、`exclude_docs` 或迁出 `docs/`，不能依赖 `.gitignore` 隔离。
- 仍作为 Markdown 正文发布、仅不显示在导航中的兼容页列入 `not_in_nav`。小测合并后的旧网址使用已有 `resource_aliases` 构建机制，按[回忆卷制作规范](RECALL_PAPER_GUIDE.md)维护目标锚点、导出范围和原页讨论，不另行复制正文。

## 本地验证

默认按实际改动选择代表样本，不机械重复全站逐页检查。共享逻辑或构建配置的影响超出代表样本覆盖范围，或样本检查发现系统性问题时，应按受影响范围扩大验证，并简要记录原因。不应仅因触及共享文件就自动进行全站逐页扫描。

使用 Python 3.12 和仓库中的固定依赖：

```powershell
py -3.12 -m venv .venv
.\.venv\Scripts\python.exe -m pip install -r requirements.txt
.\.venv\Scripts\python.exe -m mkdocs build --strict
```

按实际改动确认以下项目；纯文档文字调整只检查内容、链接和差异，结构、样式、交互或构建配置变化再运行严格构建与相应显示检查：

- 新增或修改的本地链接能够解析；
- 导航目标存在，草稿和内部文件没有进入生产构建；
- 页面在桌面端和手机端均可阅读；
- `git diff --check` 没有空白错误；
- 差异中没有夹带其他任务或原始私有材料。

小测合并或打印功能变化时，按[回忆卷制作规范](RECALL_PAPER_GUIDE.md)选择少量代表材料，走通资料入口、当前材料、组合范围与实际 A4 输出。已确认的答案只做与本次变化有关的完整性回归，不重复全量知识审查；打印结果不能仅凭屏幕预览判断。

## Git 与提交

- 只暂存本次明确范围内的文件，不使用 `git add -A`。
- 同一授权范围内、目的相关的修改优先合并审阅，不按课程或文件机械拆分 PR；提交按能清楚说明和独立回退的主题组织。
- 大幅重组、批量迁移和行尾规范化应与普通内容修改分开。
- 提交、推送、创建 PR 和发布前，先再次核对文件清单和差异范围。
