# 课程主页重构与维护指南

更新：2026-09-05

本指南用于 BMS Database 的课程主页审阅、重构和年度维护。以下内容是编辑与核查原则，不要求所有课程套用同一份固定模板。通用贡献流程见 [CONTRIBUTING.md](CONTRIBUTING.md)；原始材料、逐页核对记录和历史候选由维护者另行保存。

## 页面要解决的问题

课程主页首先帮助后来者判断：

- 这门课实际上讲什么、怎样组织；
- 平时有哪些不能拖到期末的任务；
- 分数怎样构成，近年考试有什么变化；
- 愿意平时跟课时怎样学，时间不足时最低限度怎样补救；
- 哪些考试、小测、笔记或站外资料真正可用；
- 选课前需要什么知识，教材是否值得使用。

课程介绍可以保留，但不能压过考核、学习经验和资料入口。语言不必追求最短；只要层次清楚，可以保留真实、自然的学生口吻。

## 内容依据与时效

1. 课程通知、课程导航、教学日历、实际课件和任务要求优先用于确认课程事实。
2. 课程大纲通常形成较早，不用它单独补写当前“课程学习内容”；只有与近年实际教学材料一致时才可辅助核对。
3. 学习建议只从已登记的个人经验或用户确认中整理，不为了让文字连贯而补出来源没有的结论。
4. 分数、任务次数、考试语言和题型必须带上适用学年、年级或班次；未知比例用“—”或在正文说明，不自行补足到 100%。比例合计不足 100% 时只展示已经确认的部分并说明证据边界；合计超过 100% 时先回查总项与子项、加分项或材料冲突，若原始记录确实如此则照实说明，不擅自改成 100%。
5. 对课程内容、考核和资料状态的时效限定应靠自然文字说明，不把旧学年经验写成永久规则。

## 稳定信息与逐年信息

课程实质和主要知识结构通常相对稳定，可以在正文中统一说明，不必带学年。较详细内容放进折叠块，既方便选课前了解，也不压过学习经验。

以下内容更容易变化，应按学年或班次分别记录：

- 分数比例与任务次数；
- 出勤、讨论、报告和实验要求；
- 开闭卷、考试语言、考试时长和题型；
- 允许携带的资料；
- 实验考试平台、题库和组织方式。

只有实际教学内容发生明显变化时，才更新课程内容正文；不要仅因新增一个学年就复制整份课程介绍。

## 推荐阅读顺序

材料充分时，可以参考以下顺序：

1. 课程实质；
2. 课程学习内容；
3. 上课与平时任务；
4. 分数构成；
5. 期末考试或近年考试变化；
6. 学习建议；
7. 资料；
8. 课程信息。

栏目名称和顺序可以按课程调整。没有可靠内容的栏目直接省略，不用“待补充”或通用文字填满。课程实质只回答“这是一门什么课”，考试细节放在后面的对应栏目。

## 课程学习内容

- 开头先用一小段概括课程主线，再用 `??? note "展开查看较详细的课程内容"` 收纳较详细主题。
- 折叠块按知识关系或实际教学模块分组，不抄课件目录，也不列每次上课日期。
- 只概括课件、教学日历或实际安排明确覆盖的内容；材料覆盖不全时不要自称完整课程目录。
- 课程内容通常不带学年；若依据只覆盖某一学年，应在说明中交代边界。

## 分数构成、任务与考试

- 已核对的分数构成使用 `course-score`；不同学年、年级或班次使用 MkDocs Material 标签页。
- 跨学年的同类项目尽量保持相同顺序，方便读者比较；名称、次数和比例仍按原材料保留。
- 分数构成是方便快速比较的摘要层。每个项目宜保留一行短说明，用来交代次数、形式、作用或最关键的学年差异，避免只剩项目名称和比例；小字通常只承担一个主要意思，约 20 个汉字、桌面不超过两行可作为人工检查参考，但不能用固定高度或省略号截断完整内容。
- 真正属于分数的信息，例如内部比例、次数或足以区分学年的关键形式，可以留在卡片中；任务流程、截止时间、签到或监考方式移到“上课与平时任务”，开闭卷、题型、考试语言、范围和可携带资料移到“期末考试”或近年考试说明。
- 跨学年相同的任务要求在标签页外统一写一次，标签页内只保留变化。某一学年的重要细节较多时，可以在该学年附近集中写一段“考核说明”，不要给每张卡片分别增加折叠块。
- 卡片中独有的信息不能为了缩短版面直接删除：先判断是否应迁移到正文。对读者帮助很小的细节可以经过明确取舍后省略，但这是内容判断，不是样式调整；来源和取舍边界仍应留在外部维护记录中。
- 不把任务已经布置等同于它一定单独计分，也不从剩余比例推算未知占比。
- 课程主页只概括考试形式；完整题目仍放在 `exams/` 或 `quizzes/`。

### `course-score` 的视觉约定

- 组件采用“数字边注＋文字”的轻量布局，不增加整块背景、圆角、阴影或粗边框。百分比是唯一的强调色，使用主题变量 `var(--md-typeset-a-color)`，会随明暗主题变化；不要在单门课程中把它改灰、写死色值或另设一套颜色。
- 项目名称使用正文色，短说明使用较浅文字色。桌面端自动排成两列，奇数个项目的最后一项横跨整行；小屏自动改为单列。换列、分隔线和间距均由 `docs/css/styles.css` 的共享样式处理，页面中不要手工插入 `<br>`、`<hr>`、固定宽高或课程专用 CSS。
- 固定结构为 `.course-score` → `.course-score__items` → `.course-score__item`。列表容器保留 `role="list"` 和能说明完整适用范围的 `aria-label`，每项保留 `role="listitem"`；百分比写作 `30<span>%</span>`，未知比例只写 `—`，不附 `%`。
- 多学年、年级或班次放在 Material 标签页中，标签必须足以区分实际材料；组件整体缩进在对应标签页下。标签已写明范围时可以省略可见的 `.course-score__year`，但 `aria-label` 仍要完整。只有一个适用范围时可直接显示组件，不必为了外观创建单个标签页。

### `course-score` 编辑与验收清单

1. **确认项目和比例。** 只放入有依据且能确认会计分的项目，先区分总项与内部子项，避免重复相加。整体比例已知而内部拆分未知时保留整体一项，不强拆子卡；整套比例都无法确认时不用 `course-score`，改在正文说明考核环节和证据边界。
2. **写一条真正有用的短说明。** 每项只承担一个主要意思，优先保留次数或内部比例，其次选择足以区分学年的形式、作用或变化；标题、百分比和标签页已经表达的内容不重复。没有可靠补充信息时可以省略 `<p>`，不能为了视觉整齐制造套话。
3. **把完整细节放回正文。** 任务流程、截止时间和提交方式放入“上课与平时任务”；开闭卷、语言、时长、题型、范围和可携带资料放入考试说明。卡片可以重复正文中最值得扫读的一句，但完整说明只写一次；迁移后逐项确认原卡片中的独有事实仍然存在。
4. **先调整信息层次，再处理高度。** 双栏中一条长说明会自然抬高同排项目，应优先压缩摘要或迁移细节。必要信息仍可自然换行；禁止用 `height`、`max-height`、`overflow: hidden`、`line-clamp`、省略号、缩小字体或手工 `<br>` 裁切、挤压内容。
5. **做实际渲染验收。** 运行 MkDocs 严格构建，并至少检查桌面端、390 px 手机端、明暗主题及每个标签页；确认文字没有裁切或重叠，奇数末项、分隔线和标签切换正常，页面没有横向滚动。

## 学习建议

先整理所有同学都适用的共同提醒，再按实际材料决定是否分成：

- **愿意平时跟着学的同学：**说明听课、智云、课件、笔记、小测、实验或讨论课怎样配合；
- **只想在期末补天的同学：**给出时间不足时的优先级和最低可行方案，同时说明哪些平时任务已经无法补回。

两条路径不是强制栏目。经验不足时宁可保留一段具体建议，也不要生成泛泛的学习方法。

编辑时重点处理：

- 把真正通用的建议放在分流之前，不误归到某一类同学；
- 合并重复观点，但不要为了简洁把人的语气完全磨掉；
- 允许使用“裸考”“玩崩”等自然表达，只要事实边界清楚；
- 补天建议可以写“至少不要留白”等具体做法，但不能写成稳过或取得某分数的保证；
- 对 AI 总结、前人笔记等资源只作有依据的质量提醒，不把推测写成普遍事实。

## 课程信息

有内容时统一按下列顺序排列，没有材料的项目省略：

1. 开课与学分；
2. 预修要求；
3. 实际学习需要；
4. 提前或延后修读；
5. 授课与考试用语；
6. 参考教材；
7. 其他说明。

浙江大学培养方案中的预修要求用于提示知识衔接，不能自行写成选课硬门槛；若某门课另有明确限制，再按原通知说明。预修要求与“实际上需要多少基础”分开，允许选课也不等于推荐提前修读。

预修课程只有在本站存在精确对应页面时才加站内链接。泛称“化学”、大学物理或没有主页的课程保留普通文字，不误链到名称相近的其他课程。

教材要说明实际用途：用于跟课、补充理解、复习还是开卷参考，以及是否需要购买。课程指定教材、教师推荐书和同学使用的参考书不要混写。

## 资料入口与外观

- 多项资料使用 `course-links`，按考试、小测、笔记、讨论课等实际类型提供入口。
- 站内目录链接写成 `exams/`、`quizzes/`、`notes/`，不在卡片中保留 `index.md`。
- 同学根据记忆从教师课件中找图重建的测验材料，必须明确标为回忆整理，不称为原始测验 PPT 或原题图；展示时长、题序等信息也按整理者记录或回忆表述。
- 站外资源与站内整理资料分清；受传播限制的课件只能用于内部核对，不能因此加入下载。
- 优先复用现有 `course-score`、`course-links`、标签页和折叠块，不为单门课创建独立视觉体系。
- 桌面端与手机端都要检查标题、标签切换、折叠内容、分数布局、资料卡片和横向溢出。

## 修改稿与审阅方式

修改稿要同时满足两个目标：审阅者能方便地比较同一处内容，维护者能可靠地还原和发布。默认使用“Git 差异为主、分块对照为辅”的流程，不把多份完整正文纵向堆在同一个 Markdown 中。

### 小范围修改

错字、措辞、单个段落或少量排版调整，可以在专用分支或独立 worktree 中直接修改正式文件，并先保持未提交。开始前先检查目标文件自身是否已有未提交改动；若有，冻结任务开始时的实际内容，不能把 `HEAD` 当作精确原稿。已有改动的从属或起点无法确认时，先停留在外部候选阶段，不写回正式页。只有目标文件干净且原稿已经在 Git 中时，现有提交才可以直接作为基线，不需要再提交一份相同原稿。审阅时先比较本轮修改与冻结原稿；完成全部修改后，再按指定文件检查累计 Git 差异。

### 大幅重组或证据复杂的修改

先在外部任务目录保留三种角色：

- `base.md`：开始审阅时冻结的原稿，不在后续轮次中改写；其 SHA-256 记入审阅记录；
- `candidate.md`：唯一的当前候选，根据审阅意见原位更新，不在文件末尾追加第二套全文；
- `review.md`：只列实际发生变化的语义块，每块按“原文—建议稿—修改说明或证据边界—决定”排列，并使用 `R01`、`R02` 等稳定编号。

现有 `drafts/docs/.../index.md` 可以继续承担 `candidate.md` 的角色，不必为了文件名统一破坏相对链接。若必须保存某一轮完整稿，优先使用 Git；外部审阅阶段的里程碑稿放入 `history/v001.md` 等独立文件。当前候选和预览输入始终只有一份。

已经存在的多版本候选继续作为历史档案保留，不批量拆分或删除；后续修改使用唯一当前候选，不再向旧文件追加完整版本。

## 从审阅到正式页面

1. 读取 `CONTRIBUTING.md`、本指南、相关课程材料和最新候选；涉及试卷或小测时同时读 `EXAM_SEMESTER_REFERENCE.md`。
2. 检查 Git 状态，记录并保护工作区已有修改。
3. 逐段核对课程事实、个人经验和来源不明内容。
4. 根据改动范围选择原位 Git 审阅或 `base.md`、单一当前候选与分块 `review.md`，明确审阅基线与修改范围。
5. 维护者逐块确认后，只把已接受内容写入正式页；若历史候选仍是多版本文件，只提取明确确认的版本，不能整份复制。
6. 删除 `course-info-review` 等编辑注释以及没有正文的空标题。
7. 审阅本轮 Git 差异；处理逐行批注时只改对应位置。最终检查指定文件的累计差异，确认没有夹带其他任务。
8. 将主页依赖的考试、小测、图片和导航一并纳入，先检查本地链接。
9. 运行 MkDocs 严格构建，并检查桌面、手机、明暗主题、标签页与折叠块。
10. 对照候选与正式页，确认除正式标题外内容一致；再检查 `git diff --check` 和最终文件范围。
11. 在项目维护记录中保存变更原因、来源依据和验证结果。提交前先核对拟暂存文件与差异范围；PR 与合并遵循项目维护者确认的流程。

## 年度更新清单

收到新一学年材料后，优先补充：

- 教学日历或新课件：判断课程内容是否真的发生变化；
- 课程组通知：更新任务、分数构成和开闭卷规则；
- 新试卷、小测或回忆：补资料页并更新考试概括；
- 新同学经验：只有带来新的适用条件、踩坑或学习路径时才整合进主页；
- 失效入口和资料权限：确认站内目标、站外链接及是否仍可公开。

年度更新通常只需要新增一个学年标签或修正局部文字，不应重新生成整页。
