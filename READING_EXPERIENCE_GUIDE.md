# 阅读体验维护指南

适用于网站字体、字号、标题层级、组件文字、阅读样式切换与屏幕／打印边界的调整。本指南保存可随仓库共享的约定及修改入口，使用仓库相对路径与公开链接。本机目录、预览地址、测试脚本及过程截图留在维护者的本地记录中，不作为阅读本指南的前提。后续确认新方案时更新对应条目，不连续追加多套现行规则。

## 调整原则

- 先结合实际页面判断问题来自字形、字号、密度、对比度还是组件层级，不把“费眼”直接等同于字号太小。
- 当前方案是继续微调的基线，不是不可修改的标准。一次优先比较少数相关因素，尽量固定页面、窗口、缩放和明暗主题，避免同时改动多项后无法判断原因。
- 标题与正文可以使用同一宋体家族，通过字号、字重及段前后留白建立层级。不要因为品牌要求加粗，就把所有标题一并加粗。
- 按内容角色维护共享样式；新增课程入口、练习链接、来源说明也应纳入已有规则，不为单个站外平台另建字号体系。页面正文不散落内联字体或字号覆盖。
- 参考其他网站时可以比较排版方法，但以本站读者反馈为依据，不把某个网站或某次个人观感当作普遍结论。
- 宋黑搭配按阅读内容与操作文字分工，以完整内容块保持连贯；不因文字位于卡片或提示框内就统一换字体，也不把一块短说明逐行拆成不同字体。比较时明确字体家族、字重和字号分别改变了什么，并核对实际字形。
- 一次全宋体或宋黑搭配的个人观感不代表普遍最优。后续试验与已确认规则分开记录，确认后更新下方角色表。

## 修改入口

| 文件 | 负责内容 |
| --- | --- |
| `docs/index.md` | 首页项目介绍、合作练习入口和来源说明 |
| `overrides/.icons/ginkgo.svg` | 页脚银杏叶图标，内含来源、适配说明和 MIT 许可 |
| `docs/css/styles.css` | 新版正文与界面字体、组件字号变量、切换按钮样式，以及现有课程组件 |
| `docs/css/resource-export.css` | 资料列表与打印入口组件；同一文件后部还有独立打印稿样式，修改时区分作用域 |
| `docs/javascripts/reading-style.js` | 新旧偏好初始化、按钮、选择记忆与页面切换后的挂载 |
| `overrides/main.html` | 在页面头部同步加载偏好脚本，先恢复选择再绘制正文 |
| `docs/javascripts/resource-export.js` | 为对应资料正文设置 `.reading-paper`，以及原有打印／导出入口 |
| `docs/assets/fonts/noto-serif-sc/` | 站内宋体阅读字体、分片 CSS、授权、来源说明与校验清单 |
| `docs/assets/fonts/noto-sans-sc/` | 站内黑体界面字体、分片 CSS、授权、来源说明与校验清单 |
| `docs/css/header-wordmark.css`、`docs/assets/fonts/brand/` | 页头及手机抽屉的品牌宋体，与正文阅读字体分开维护 |
| `overrides/partials/header.html` | 图标、双层页名与品牌首页链接结构 |
| `overrides/partials/comments.html` | 评论区明暗主题与即时导航后的样式地址 |
| `mkdocs.yml` | 字体及样式加载顺序、主题配置与 `extra.social` 页脚链接；阅读偏好脚本通过模板加载，不重复登记 |
| `docs/resource-export.html` | 独立打印／导出页面，与普通文章页分开检查 |
| `hooks/resource_usage.py`、`overrides/partials/copyright.html` | 构建时的资料标识、正式来源页尾及全站使用说明入口 |
| `docs/images/zjubms-*.svg` 与同名前缀 PNG | 当前图标及按配色和背景保留的样式，SVG 与两种尺寸 PNG 同步维护 |

开始修改时查看实际分支和这些文件的状态，确认已包含最新发布内容。混合工作区的旧检出不一定反映已发布版本；优先复用合适的隔离工作树并核对差异，不覆盖其他任务改动。

## 当前方案

当前新版默认采用宋黑搭配，并保留原版／新版切换。后续按新的反馈更新这些规则。

### 字体角色

| 内容 | 当前字体 |
| --- | --- |
| 普通正文、来源说明、各级 Markdown 标题、评分说明及提示框内容 | 站内 `BMS Noto Serif SC`，中文、英文和数字采用该家族配套字形 |
| 已分类的试卷、小测正文 `.reading-paper`，包括答案与解析 | 英文和数字优先 `Times New Roman`，中文使用站内宋体；缺少 Times New Roman 时按字体栈回退 |
| 导航、搜索、按钮、筛选器、标签页及折叠开关 | 站内 `BMS Noto Sans SC`，中英数字使用该家族配套字形；常规字重 400，重点界面角色 500 |
| 课程入口卡片、资料目录卡片 | 标题及说明作为完整内容块使用站内宋体；资料卡片独立的阅读、打印、练习操作及状态标签使用界面黑体 |
| 课程目录、培养方案课程列表 | 整组界面黑体，课程名使用 500；列表外的章节标题、说明和培养方案解释段使用宋体 |
| 滚动页头的当前页名、页脚前后页的页面名称 | 站内宋体；“上一页／下一页”、版权及其他功能文字使用界面黑体 |
| 页头和手机抽屉中的 BMS Database 品牌 | 独立的 `BMS Brand Serif`，字重 700 |
| 公式、代码 | 各自的 KaTeX 和等宽字体规则 |

`.reading-paper` 当前只自动识别 `mandatory/` 或 `elective/` 下，课程 `exams/`、`quizzes/` 中的直接资料正文页；分组目录、下载文件、普通主页和讨论课病例不在此范围。正文识别先于目录处理，资料正文末段即使也叫 `quizzes` 或 `exams`，仍应添加试卷类和打印入口。以后增加其他题目路径时，应明确检查分类逻辑，不能假设标题中带“题”就会自动适用。

新增入口需同时考虑容器和独立节点。例如首页 Ginkgo 使用独立 `.course-link`，应与 `.course-links` 内的入口共同遵循完整卡片宋体规则；卡片外的 `.course-source-note` 也按正文角色处理。资料目录的 `.resource-index-actions` 等操作行单独使用界面黑体，不把整个 `.course-resource-grid` 改成黑体。

正文标题与目录条目分开处理：`.course-catalog-group` 的章节标题保留宋体，内部 `.course-catalog` 使用黑体；`.curriculum-context`、`.curriculum-plan-summary` 作为解释内容整块保留宋体。`details > summary` 是展开控件，使用黑体；折叠后的阅读内容继续继承所在正文或试卷的字体。普通提示框的标题与内容保持宋体，不按边框外观判断字体角色。

### 字号与层级

| 参数 | 普通屏幕 | 窄屏 `max-width: 44.984375em` |
| --- | --- | --- |
| 正文字号 | `15.5px` | `15px` |
| 正文行高 | 继承主题 `1.6` | 继承主题 `1.6` |
| `--bms-reading-unit` | `19.375px` | `18.75px` |
| `--bms-reading-small-unit` | `20px` | `20px` |

顶部一级菜单 `.md-tabs__link` 在显示时固定为 `15px`；左右导航、搜索及其他界面的字号仍按各自规则维护，不联动放大。

正文不会因超宽屏主题根字号变大而再次放大。标题沿用主题相对字号、字重及留白层级；例如普通文章在桌面基线下 H1 约 31px、H2 约 24.22px，不是额外硬编码的标题尺寸。

组件使用 `calc(系数 * var(--bms-reading-unit, 1rem))` 或小字变量。后者让原本较小的辅助文字保留常规尺寸，不再随宽屏继续放大。选用哪一个取决于文字角色和实际显示，不把本轮旧字号换算时的阈值当作新组件的固定规定。保留 `1rem` 回退，布局间距、点击区域和根字号不随阅读字号一起缩放。

### 局部文字的处理

- 课程目录及培养方案的状态标签、目录分类统计在新版使用 `11px`；统计文字使用主题浅色前景，不再用更浅一级的颜色。课程名称仍沿用原阅读单位，重点界面字重为 500。
- 培养方案 `h5[id^="curriculum-"]` 使用 `0.95em`、常规前景色和既有粗体，约 `14.725px`／`14.25px`；标题中的学分标签仍作为辅助信息。其他页面的标题不因此整体加粗或放大。
- 连续答案和解析明确标记 `.answer-explanation`，新版为 `14px`、行高 `1.6`。优先为各解析段落添加 Markdown 属性列表，保留段落在原折叠区的位置，避免新增包装层改变原版和打印稿的首末段间距；不按字符数猜测解析，不整体放大 `details`、折叠标题或纯字母短答案。
- 需要持续阅读的长文字表格用 `.reading-table--prose` 包裹，新版为 `14px`，整张表的最小宽度为 `42em`，窄屏在表格区域内横向滚动。包装区域标明用途并可键盘聚焦；短数据表保持既有规则，不因行数多就使用此类。
- 以上局部覆盖只作用于新版屏幕阅读；原版和独立打印稿继续各自的规则。卡片、提示框和答案框仍按上方内容角色分配字体，不因调整字号而改变整个容器的字体。

卷首考试信息与资料说明分别作为完整内容块展示，避免同一条要求在信息栏和提示框重复。说明用语、分隔线和小测日期格式按[回忆卷指南的卷首说明](RECALL_PAPER_GUIDE.md#卷首说明的统一写法)维护，继续复用现有引用栏与提示框样式。

### 资料卡片的操作层级

- 带 Ginkgo 练习的资料卡片以原资料标题为主要入口：标题使用常规正文色和较明显的字重，练习操作使用主题链接色，打印操作使用较浅前景色；悬停和键盘聚焦沿用主题反馈。颜色使用变量适配明暗主题，不把全部文字都改成链接色，也不为 Ginkgo 另设字体或字号。
- 样式集中在 `docs/css/resource-export.css`，以 `.resource-practice-link` 和已生成的标题链接限定作用范围。保留原课程名、站外、老师版等状态标签，Ginkgo 的统一 `.course-source-note` 放在卡片组下方。隐藏重复阅读操作的条件与导出边界见[回忆卷制作规范](RECALL_PAPER_GUIDE.md)；字体继续按上方内容和操作角色分工。

### 首页合作入口与页脚友情链接

- 首页保留项目介绍和独立 `.course-link`，链接到 [ZJUBMS Ginkgo 在线练习站](https://yangshu233-snow.github.io/ZJUBMS-Ginkgo/)。来源说明使用 `.course-source-note`，写清练习版可能补充选项和参考答案，原始回忆仍以对应资料页为准。首页允许自然滚动，不为单屏容纳删减必要说明或挤压文字。
- 页脚友情链接复用 `mkdocs.yml` 的 `extra.social`，以小图标与基础医学院官网入口并列；Ginkgo 图标链接到 [Ginkgo 项目仓库](https://github.com/YangShu233-Snow/Ginkgo)，悬停名称为“Ginkgo · GitHub 仓库”。首页练习入口和页脚项目链接分别维护，不互相替换，也不另加文字友链行或全站导航条目。
- 图标位于 `overrides/.icons/ginkgo.svg`，沿用项目自带银杏叶的图形路径，去白底并使用 `currentColor` 适配页脚。更新图标时保留来源、修改说明和 MIT 版权许可；保持可辨识的形状、点击区域及悬停名称，并检查桌面、手机和明暗主题。

### 页眉图标

- 各款 BMS 图标统一使用[浙大校标文件](https://www.zju.edu.cn/572/main.htm)中的求是鹰矢量轮廓，等比缩放，不改变鹰形比例；主蓝采用官网公布的[求是蓝](https://www.zju.edu.cn/english/2023/0323/c74903a2732011/page.htm) `#003F88`。BMS 纸带沿用本站已确认的字形与间距；BMS 字形为本站重绘，不称为学院原始矢量
- 页眉使用透明底白鹰 SVG，BMS 正面为求是蓝、背面为中蓝，图片由 `theme.logo` 指定；标签页独立使用蓝鹰透明底 PNG，纸带正面为白色、背面为浅蓝。中蓝 `#7395BE` 和浅蓝 `#BFCFE1` 分别由求是蓝混入 45% 和 75% 白色得到，是纸带辅助色。条纹与纯色款按纸带配色和背景命名保留，各款字母尺寸一致
- 桌面图标为 36px，`max-width: 44.984375em` 下为 24px；小于 360px 时保留纯文字页眉
- 图标与站名共用首页链接，保留 Material 的两层 topic 和组件标记；新版中滚动后的页名使用站内宋体，原版沿用主题字体。图标使用独立样式类，抽屉中的横图保持隐藏
- SVG 作为可维护的图形源，1254px 与 512px PNG 按同一版本同步生成；更新时核对鹰形等比、字母比例与间距、透明边界及各背景下的小尺寸显示。样式文件名说明纸带配色与背景，来源图和替换前图稿保留可追溯记录，不以日期或 legacy 作为样式名称。

### 题图尺寸

- `.exam-figure` 与 `.quiz-figure` 在 `docs/css/styles.css` 中共用居中、最大宽度和等比例缩放规则，适用于新旧阅读样式；不将所有正文图片、行内图标一并改成块级居中。
- 单图尺寸通过 Markdown 的 `width` 属性调整，缩小后可链接原图；具体写法和选取原则见[回忆卷制作规范](RECALL_PAPER_GUIDE.md#3-图片)。样式类控制公共行为，页面只记录必要的单图宽度。
- 独立打印稿保留其 `resource-export.css` 规则。页面的宽度属性会传递到导出，尺寸试改需同时检查屏幕与导出；不为统一外观把屏幕宽度上限复制进打印稿。

### 临时新旧切换

- 保留页头原版／新版切换，首次访问默认新版宋黑搭配，并记住选择。该按钮仍用于收集阅读体验反馈，尚无自动移除日期；将来改名或撤除时按新的反馈处理，不在其他维护中顺手移除。
- `html[data-reading-style="original"]` 恢复本次字体试验前的正文与界面字体、字号及组件单位，包括原主题界面字体和顶部菜单字号；它不回退后来单独发布的页眉图标、品牌字体或页面内容，也与明暗模式独立。
- 新版覆盖与变量仅在新版 `@media screen` 中启用。沿用 `:where(html:not([data-reading-style="original"]))` 等低优先级作用域，避免为了覆盖单个组件而提高全站选择器优先级。
- 偏好键为 `bms-reading-style:<站点根路径>`。保留首次默认新版、无效／禁用存储降级、跨标签页同步、键盘操作和 Material 即时导航下只挂载一个按钮的行为。
- 后续若撤除切换，应一起梳理模板脚本、数据属性、CSS 条件、变量回退和旧存储值，先明确旧选择如何迁移，避免只删按钮留下隐式旧模式。

### 字体资源与打印边界

正文宋体和界面黑体分别由 `docs/assets/fonts/noto-serif-sc/` 与 `docs/assets/fonts/noto-sans-sc/` 的站内 WOFF2 分片提供，使用独立别名、相对地址、`unicode-range`、可变字重和 `font-display: swap`。不依赖维护者本机恰好安装的字体，也不把 Noto 网页字体改回运行时 Google 请求。更新时分别维护 `README.txt`、`OFL.txt` 和 `SHA256SUMS.txt`，核对真实字形和授权来源；不要将 Windows 系统字体文件直接作为网页资产加入。宋体另含希腊字母与 `ⅠⅡⅢⅣⅤ` 的官方小分片；仅扩大 CSS 的范围而未核对文件中的字形，不能视为补齐。

字体名称字符串只说明 CSS 期望值，不能证明实际命中字形。需用浏览器字体检查确认宋体和黑体的中文、英数、希腊字母、课程名中的罗马数字及实际使用字重；新设备没有 Times New Roman 时应记录回退情况。原版不应主动请求正文或界面的 Noto 分片，但独立品牌字体仍可能加载；切换前已加载的字体可以留在浏览器缓存中。字体说明使用 `.txt`，避免位于 `docs/` 的 README 被构建为多余网站页面。

阅读变量只作用于屏幕。独立 `resource-export.html` 沿用自身 CSS、字体栈和 pt 字号，不加载阅读偏好脚本或自动接入正文、界面网页字体。`BMS Noto Serif SC`、`BMS Noto Sans SC`、打印栈中的 `Noto Serif CJK SC` 及品牌别名是不同的 CSS 家族。调整普通阅读体验时保持打印稿边界；需要改善打印时按实际授权另行修改并核验产物。

## 两侧导航与页内目录

- 左侧导航在所属课程和资料分组下使用简短名称，保留学年、已确认学期及原卷、回忆整理等必要区别，不重复上层已说明的课程名。正文标题仍保留完整名称。
- 右侧目录用于定位章节，试卷题型只保留名称及年份、A/B 卷、I/II 型等必要区分。分值、题量和选答说明留在正文，题型标题显示有依据的中文总分；分值计算、独立标签的语言及原有英文说明的保留边界，统一按[回忆卷指南的分值标注](RECALL_PAPER_GUIDE.md#分值标注)处理。使用 Markdown 的 `data-toc-label` 设置目录文字；修改标题时显式保留原有锚点，不只在桌面端用脚本删字。中文站点标题与导航保持原有语言。
- 试卷、小测合集的目录目标若位于未选中的标签页，点击或直接打开章节链接时先展开目标所属标签；共享行为在 `course-navigation.js` 维护，不改变培养方案的标签与目录筛选规则。
- 培养方案的班型标识以[课程名称与目录映射](COURSE_NAME_MAP.yml)为准，同步维护 `data-curriculum-plan`、`curriculum-<年级>-<班型>-<章节>` 和映射中的 `curriculum_plans`。新目录与新链接使用当前标识；已发布的旧章节 ID 留在对应标题内的空 `<span id="旧ID" aria-hidden="true"></span>` 中作兼容别名，保留原值，避免替换成新 ID 后产生重复。
- 修改培养方案标识后，逐个检查年级和班型的标签选择、当前目录与标题前缀，以及新旧 ID 的唯一性和对应关系。跨班型直达是否展开目标标签须在新的浏览器上下文中实际验证，不能仅凭旧 ID 存在判定；保留既有标签与目录筛选逻辑，跳转行为调整另行处理。
- 调整后同时核对桌面右侧目录和手机页内目录，检查换行、跳转及无脚本时的目录文字；不靠固定行数裁切长名称。

### 资料使用说明与打印来源

- 资料使用标识由 `hooks/resource_usage.py` 写入构建后的正文，标题下仅保留短提示；补充的既有许可说明并入卷末来源首行，不再独占标题下一行。使用小字变量和主题颜色，打印入口与提示在空间足够时同排，窄屏自然换行并收紧间距；两者仍是独立节点，打印时只隐藏操作入口。默认中立说明与逐页启用的限制分开，具体范围见[回忆卷制作规范](RECALL_PAPER_GUIDE.md)
- 全站页脚的“资料免费获取 · 使用限制”入口位于 `overrides/partials/copyright.html`，与原主题署名同排，保留原有两行结构，不另增提示行；不把资料限制附加到代码或独立许可资产
- `.resource-use-notice` 和 `.resource-use-source` 的网页、独立导出样式集中在 `docs/css/resource-export.css`；导出使用自身 pt 字号，不引入阅读偏好
- 独立导出保留每份正文已有的标识和正式来源地址，答案集中卷末时来源说明排在答案之后。打印建议放在操作说明附近，打印时随工具栏隐藏；原文地址和使用限制属于正文，打印时保留
- 不使用覆盖正文的水印或依赖逐页重复页脚的方案；修改时核对电脑/手机、明暗主题、无脚本、即时导航和实际单份/批量 PDF，包含长标题、题图与三种答案模式

## 后续验证与记录

按实际改动选择检查范围，不每轮重复全量构建和截图。纯说明或局部文字记录只核对文字与链接；修改样式、结构或交互时，在最终候选上集中构建与检查。

- 字体／字号：选择有普通段落、各级标题、评分或提示框的主页，以及课程目录、资源目录和试卷正文；检查正文、完整卡片、操作行、辅助文字、独立入口和按钮是否各自命中预期字体及字重。新资源还需核对分类、长答案和长文字表格的语义标记，不因路径或组件相似就假设已适配。
- 适配：检查电脑、手机和明暗主题；改字号变量时补查宽屏，涉及页头时补查窄屏。关注行长、断行、层级、点击区域和横向溢出，不只比计算字号。
- 开关／模板：核对新旧两种模式、首次访问、刷新与跨页记忆；改相关交互时补查键盘、存储失效、跨标签同步和真实即时导航。原版应与本轮开始时的原版基线比较。
- 共享导出样式：确认入口与原有站外练习链接仍可用，页面打印媒体不随阅读偏好变化。若实际改到打印稿规则或生成逻辑，再检查单份／批量导出及生成的 PDF；只看打印入口不能称作 PDF 已验证。
- 即时导航：本地预览的 sitemap 若仍带公网根路径或端口，可能退回整页跳转；刷新后偏好保留不能证明即时导航正确。按实际站点路径验证，测试中的地址映射不要带入业务实现。
- 评论区：样式目录在页面脚本首次执行时解析为绝对地址，持久保留的主题监听使用该地址，不随即时导航后的页面深度重新解析。检查跨不同目录深度往返后切换明暗主题，确认样式仍指向站点的 `css/` 目录，且没有重复绑定监听。

环境准备与构建方法见 [README.md](README.md) 和 [贡献指南](CONTRIBUTING.md)。在当前工作树使用已安装项目依赖的 Python 运行严格构建；如需输出到仓库外，用 `--site-dir` 指定维护者自行选择的预览目录。构建产物不纳入提交。发布前核对范围内差异，发布后分别核对 Actions、Pages 和线上实际文件／显示；合并成功不等于网站已更新。

新试验先复用适合的当前候选和预览入口，旧稿只留作历史。每次明确改变了哪些因素、用户接受了什么，以及实际验证边界；更新本指南的当前条目。交接只保留状态和入口，不把截图、浏览器脚本或固定进程号复制成长期规则。

## 中文排版参考

- [Ant Design：字体](https://ant.design/docs/spec/font-cn/)：参考界面文字的角色、字重和层级；其应用界面默认字号不直接作为学习文章的通用标准
- [W3C《中文排版需求》草案](https://www.w3.org/TR/clreq/)：核对中文字体使用、标点、标题和中西混排的基础需求
- [The Type：孔雀计划](https://www.thetype.com/special/kongque/)：理解中文行长、网格和混排的设计思路

## 记录入口

- [PR #119：添加 Ginkgo 练习入口和友情链接](https://github.com/BMS-ZJU/BMS_Database/pull/119)
- [PR #120：调整网站字体和字号](https://github.com/BMS-ZJU/BMS_Database/pull/120)
- [课程主页维护指南](COURSE_HOMEPAGE_GUIDE.md)
- [回忆卷排版指南](RECALL_PAPER_GUIDE.md)

公开的变更背景通过 Git 历史和对应 PR 追溯。本地预览与检查记录由各维护者自行保存；其中的候选和服务状态仅代表当次环境，复用前应重新核对。
